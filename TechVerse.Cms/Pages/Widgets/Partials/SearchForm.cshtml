@model TechVerse.Cms.Pages.Widgets.Partials.SearchFormModel

<div class="search-widget mb-4">
    <div class="input-group">
        <input type="text"
               name="Terms"
               class="form-control"
               placeholder="ค้นหาข้อมูล..."
               value="@Model?.Terms" />

        <button class="btn btn-primary" type="submit">
            <i class="fa fa-search"></i> ค้นหา
        </button>
    </div>
</div>
